# ガイドライン: ここに題名

> このファイルは `docs/spec/guidelines/<名前>.md` にコピーして使うテンプレート。
> コピー後、この引用ブロックは削除する。
>
> ガイドラインは**個別の機能に属さない横断ルール**。
> 「この機能はこう動く」は仕様であり、ここには書かない。
> **この文書の変更には人間の承認が必要**（[ADR-0006](../../adr/0006-split-external-and-internal-spec.md)）。

- 適用範囲: （どのコード・どの文書に効くか）
- 関連 ADR: ADR-nnnn
- ステータス: ドラフト | 確定

## 原則

なぜこのガイドラインがあるのか。1 〜 3 文。
ルールを丸暗記させるのではなく、**判断の拠り所**を与える。

## ルール

強度を明示する。`MUST`（必須）/ `SHOULD`（原則そうする）/ `MAY`（してもよい）。

| # | 強度 | ルール |
| --- | --- | --- |
| G-nn-01 | MUST | |
| G-nn-02 | SHOULD | |

## 具体例

抽象的なルールは守られない。**必ず良い例と悪い例を並べる。**

### 良い例

```
```

### 悪い例

```
```

なぜ悪いのか: （理由を書く。「規約だから」は理由ではない）

## 例外

ルールを破ってよい条件と、そのとき何を書き残すか。
例外を認めない場合は「例外なし」と明記する。

## 未解決事項

- `TODO(未確定)`:
