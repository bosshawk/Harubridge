# 基本設計

**機能をまたいで効く構造**だけを書く。個別機能の実装詳細は書かない —— コードが正
（[ADR-0008](../adr/0008-code-as-source-of-truth.md)）。

> ステータス: **骨子のみ。** 技術構成が未決のため中身は空
> （[ADR-0004](../adr/0004-defer-tech-stack-decision.md)）。決定後に埋める。

## この文書に書くこと / 書かないこと

| 書く | 書かない（→ コード） |
| --- | --- |
| 全体をどんな塊に分けているか、その境界 | 各クラス・関数の構造 |
| 塊どうしが何をやり取りするか（約束事） | 具体的な呼び出し手順 |
| データをどこに、どういう原則で持つか | テーブル定義・スキーマ |
| 外部（ゲーム通信・OS）との接点の置き方 | 個別 API のパース処理 |
| 機能追加時に必ず従う構造上の制約 | 個別機能の振る舞い（→ [`external/`](external/)） |

判断の目安: **その記述が古くなったとき、実装者が誤った前提を持つか？**
持つなら書く。単に「今こう書いてある」だけならコードに任せる。

**なぜその構造にしたか**はここではなく [ADR](../adr/) に書く。
この文書は「いまどういう構造か」だけを現在形で書く。

## 全体構造

`TODO(未確定)`: 技術構成の決定後に記述する。

```mermaid
flowchart LR
    A[未定] --> B[未定]
```

| 構成要素 | 責務 | 他との境界 |
| --- | --- | --- |
| `TODO(未確定)` | | |

## データの持ち方

`TODO(未確定)`: 技術構成の決定後に記述する。

決まった時点で書くこと:

- 何を永続化し、何をメモリに留めるか
- 保存場所（ユーザーから辿れること）
- 認証情報を保存しない仕組み（[constraints.md](constraints.md) C-04 / C-07）

## 外部との接点

`TODO(未確定)`: 通信の観測方式が未決のため記述できない。

決まった時点で書くこと:

- 観測を行う場所と、そこから先へ何を渡すか
- **読み取り専用を構造として保証する方法**（[constraints.md](constraints.md) C-02）
- 非公開仕様への依存を局所化する境界（[constraints.md](constraints.md) C-03）
- 未知の構造を受け取ったときに縮退させる位置（停止させない）

## 機能追加時の構造上の制約

`TODO(未確定)`: 技術構成の決定後に記述する。

## 未解決事項

- `TODO(未確定)`: 本文すべて。[ADR-0004](../adr/0004-defer-tech-stack-decision.md) の解消待ち。
