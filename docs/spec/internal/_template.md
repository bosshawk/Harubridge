# 内部仕様: ここに機能グループ名

> このファイルは `docs/spec/internal/<機能グループ名>.md` にコピーして使うテンプレート。
> 外部仕様とファイル名を揃える。コピー後、この引用ブロックは削除する。
>
> **ここには「どう実現するか」だけを書く。** ユーザーから見える振る舞いは
> 外部仕様に書き、ここでは ID かリンクで参照するに留める。切り分け基準は
> [docs/spec/README.md](../README.md#外部仕様と内部仕様の切り分け) を参照。
>
> **節は削除しない。** 該当しない節には `該当なし: 理由` を 1 行書く。
> 「該当なし」（判断済み）と `TODO(未確定)`（未判断）を混同しない。
>
> **外部仕様を変えない限り、エージェントの判断で更新してよい**
> （[ADR-0006](../../adr/0006-split-external-and-internal-spec.md)）。

- 対応する外部仕様: `docs/spec/external/<同名>.md`
- 拘束される要求: NFR-nnn（この内部仕様が満たすべき非機能要求を列挙する）
- 関連 ADR: ADR-nnnn
- ステータス: ドラフト | 確定

## 構成

責務の分割と、それぞれが何を知っているか。

```mermaid
flowchart LR
    A[モジュールA] --> B[モジュールB]
```

| モジュール | 責務 | 依存先 |
| --- | --- | --- |
| | | |

## データモデル

保持するデータの構造と、その寿命（メモリのみ / 永続化 / キャッシュ）。
保存**形式**はここに書く。保存**場所**は外部仕様（NFR-008）。

| 名前 | 構造 | 保存先 | 寿命 |
| --- | --- | --- | --- |
| | | | |

## 処理フロー

主要な処理の流れ。分岐と、どこで失敗しうるかを示す。

## 入力データの取得元

| 項目 | 型 / 形式 | 取得元 | 根拠 |
| --- | --- | --- | --- |
| | | | `TODO(要検証)` |

> 艦これの API 構造は非公開のため、取得元には**根拠（実測日 / 参照 OSS）を必ず書く**
> （[constraints.md](../constraints.md) C-03）。根拠が無い記述には `TODO(要検証)` を付ける。
> 参照した OSS がある場合はライセンス表記を確認する（C-07）。

## エラー処理と縮退動作

状況の定義とユーザーに見える結果は**外部仕様の `E-nn` が正**とする。
ここには**検知方法と内部処理だけ**を書き、状況を文章で再掲しない。

| 外部仕様 ID | 検知方法 | 内部の挙動 |
| --- | --- | --- |
| E-01 | | |
| E-02 | | |
| E-03 | | 停止しない。未知のフィールドは無視して既知の範囲で処理を続ける（NFR-003） |

ユーザーに見えない内部エラーは ID を `なし` とし、**なぜ外部に現れないか**を書く。

**ゲームサーバへのリトライ・再送は行わない**（[constraints.md](../constraints.md) C-02）。

## 性能・リソース

外部仕様の「性能に関する期待」を**どう達成するか**を書く（目標値の再掲はしない）。

- 処理頻度と 1 回あたりの想定コスト
- メモリ・ディスクの増加要因と上限（NFR-006）
- ゲーム画面の操作レスポンスに影響しないための方針（NFR-002）

## テスト方針

| 対象 | テストの種類 | 確認すること |
| --- | --- | --- |
| | | |

外部仕様の「受け入れ条件」および `E-nn` と対応させる。

## この文書を参照している箇所

この内部仕様に依存している他の文書・モジュールの一覧。
変更時に見るべき場所を引くための索引であり、影響の分析結果ではない。

| 参照元 | 何に依存しているか |
| --- | --- |
| | |

## 未解決事項

- `TODO(未確定)`:
