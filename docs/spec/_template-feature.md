# 機能仕様: ここに機能名

> このファイルは `docs/spec/features/<機能名>.md` にコピーして使うテンプレート。
> コピー後、この引用ブロックは削除する。

- 関連要求: FR-nnn, NFR-nnn
- 関連 ADR: ADR-nnnn
- ステータス: ドラフト | 確定

## 目的

この機能が何を解決するのか。1 〜 3 文。

## 振る舞い

**現在形で書く。** 実装がこの記述に一致していれば正しい、と言える粒度まで書く。

- 〜のとき、〜する。
- 〜の場合は、〜する。

## 画面 / 表示

表示する項目と、その並び・単位・フォーマット。
（Mermaid や ASCII で構造を示してよい。外部画像は使わない）

## 入力とデータ

| 項目 | 型 / 形式 | 取得元 | 根拠 |
| --- | --- | --- | --- |
| | | | `TODO(要検証)` |

> 艦これの API 構造は非公開のため、取得元には**根拠（実測日 / 参照 OSS）を必ず書く**
> （`docs/spec/constraints.md` C-03）。

## 異常系・縮退動作

| 状況 | 挙動 |
| --- | --- |
| データが取得できていない | |
| 未知のフィールド / 構造変化 | （停止しないこと。NFR-003） |
| 保存に失敗した | |

## 受け入れ条件

エージェントの実装が完了したと判定するためのチェックリスト。

- [ ] 〜が〜と表示される
- [ ] 〜のとき〜しない

## 未解決事項

- `TODO(未確定)`:
