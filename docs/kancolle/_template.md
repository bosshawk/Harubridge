# 艦これ仕様: ここに題名

> このファイルは `docs/kancolle/<api|formulas|rules>/<名前>.md` にコピーして使う。
> コピー後、この引用ブロックは削除する。
>
> **ここは外部システムの観測結果である。** 我々がどう実装するかは書かない
> （[ADR-0020](../adr/0020-kancolle-reference.md)）。

- 分類: API / 計算式 / ルール
- 最終観測: YYYY-MM-DD
- 確度: 実測 / 参照（出典あり） / 推定（`TODO(要検証)`）

## 概要

何についての記録か。1 〜 3 文。

## 内容

観測した事実を書く。**推定と事実を混ぜない。**

## 根拠

**この節は省略できない。** 根拠の無い記述は `TODO(要検証)` を付けること。

| 記述 | 根拠 | 参照日 |
| --- | --- | --- |
| | 実測 / リポジトリ名（ライセンス）/ URL | |

## 未確認のこと

分かっていないことを明示する。**空欄にしない。**

- `TODO(要検証)`:
