# ルール: ここに名称

> `docs/kancolle/rules/<名前>.md` にコピーして使う。コピー後、この引用ブロックは削除する。
> **観測結果だけを書く。我々がどう扱うかは書かない。**

- 最終観測: YYYY-MM-DD
- 確度: 実測 / 参照 / 推定

## ルールの内容

## 数値

| 項目 | 値 | 変動するか |
| --- | --- | --- |
| | | 固定 / 条件により変わる（条件を書く） |

> **数値を固定値として扱ってよいかは、別の問題である。**
> ゲームのアップデートで変わりうる（[C-03](../../spec/constraints.md)）。
> 実装がこの値をハードコードしてよいかは、ここではなく設計側で判断する。

## 変動の条件

値が変わる条件（改装・任務・課金など）。**無いなら「無い」と書き、その根拠を示す。**

## 根拠

| 記述 | 根拠 | 参照日 |
| --- | --- | --- |
| | 実測 / URL / OSS（ライセンス） | |

## 未確認のこと

- `TODO(要検証)`:
