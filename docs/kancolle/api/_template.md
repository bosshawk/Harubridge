# API: ここにエンドポイント名

> `docs/kancolle/api/<名前>.md` にコピーして使う。コピー後、この引用ブロックは削除する。
> **観測結果だけを書く。我々がどう扱うかは書かない**（[ADR-0020](../../adr/0020-kancolle-reference.md)）。

- パス: `/kcsapi/...`
- 最終観測: YYYY-MM-DD
- 確度: 実測 / 参照 / 推定

## いつ飛ぶか

どの操作の結果として発生するか。1 回の操作で複数回飛ぶ場合はその旨。

## リクエスト

| 項目 | 内容 |
| --- | --- |
| メソッド | |
| 形式 | |
| 主なパラメータ | **`api_token` など認証情報の値は書かない**（[C-04](../../spec/constraints.md)） |

## レスポンス

- 形式: `svdata=` + JSON
- 観測したサイズ:

### 主なフィールド

**すべてを埋めようとしない。** 分かっているものだけ書く。

| フィールド | 型 | 意味 | 確度 |
| --- | --- | --- | --- |
| | | | 実測 / 参照 / `TODO(要検証)` |

## 根拠

| 記述 | 根拠 | 参照日 |
| --- | --- | --- |
| | 実測 / リポジトリ名（ライセンス）/ URL | |

## 未確認のこと

- `TODO(要検証)`:
