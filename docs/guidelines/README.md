# ガイドライン

**個別の機能に属さない横断ルール**を書く場所。コーディング規約・エラー処理方針・
ログ方針・テスト方針・UI 原則など。

> **この階層の変更には人間の承認が必要**（[ADR-0015](../adr/0015-documentation-layout.md)）。
> 影響がリポジトリ全体に及ぶため。

## 仕様との違い

| | 仕様（[`docs/spec/`](../spec/)） | ガイドライン |
| --- | --- | --- |
| 対象 | 特定の機能 | 全コードに横断的 |
| 問い | 「この機能は何をするか」 | 「どう書くか」 |
| 寿命 | 機能が消えれば消える | プロジェクトと同じ |
| 例 | 遠征タイマーは残り時間を分単位で表示する | 時刻は必ず単調増加時計から取得する |

仕様とは別の軸なので、`spec/` の下ではなく並列に置く
（[ADR-0015](../adr/0015-documentation-layout.md)）。
開発の**進め方**は [CONTRIBUTING.md](../../CONTRIBUTING.md)。

## 現在の状態

**空。** ほとんどのガイドラインは言語・フレームワークに依存するため、
技術構成が決まるまで書かない（[ADR-0004](../adr/0004-defer-tech-stack-decision.md)）。

技術構成の決定後に着手する候補:

| 候補 | 内容 |
| --- | --- |
| `coding.md` | 命名・ファイル分割・コメントの方針 |
| `error-handling.md` | 例外の扱い、未知のデータに対する縮退動作の実装パターン |
| `logging.md` | ログレベル、出力先、**認証情報のマスキング**（C-04 / C-07） |
| `testing.md` | 何をテストするか、外部仕様の受け入れ条件との対応づけ |
| `ui.md` | 表示の一貫性、用語（[glossary.md](../spec/glossary.md)）の使い方 |
| `data-observation.md` | 通信の観測に関する実装上の禁止事項（C-02 の具体化） |

## 書き方

- [`_template.md`](_template.md) を `<名前>.md` にコピーして書く。
- ルールには強度（`MUST` / `SHOULD` / `MAY`）と ID（`G-nn-mm`）を付ける。
  エージェントへの指摘やレビューで ID 参照できるようにするため。
- **良い例と悪い例を必ず並べる。** 抽象的なルールだけでは守られない。
- 「なぜそうするか」を書く。理由の無いルールは、状況が変わったときに判断できない。

## 一覧

| ファイル | 適用範囲 | ステータス |
| --- | --- | --- |
| [`rust.md`](rust.md) | すべての Rust コード（`crates/` と `src-tauri/`。略号 `RS`） | **Draft（承認待ち）** |
| [`typescript.md`](typescript.md) | `src/` 配下の TypeScript / React と注入スクリプト（略号 `TS`） | **Draft（承認待ち）** |
