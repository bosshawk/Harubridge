# CLAUDE.md — エージェント作業規約

このリポジトリで作業する AI エージェント（Claude Code / Codex など）が**常に**従うルール。
人間はレビュアーであり、コードとドキュメントの記述はエージェントが行う。

## 0. 大前提

- 実装はすべてエージェントが行う。**人間はコードを直接書かない**前提で、
  読みやすさ・自己説明性・ドキュメントとの対応関係を通常以上に重視する。
- **ユーザーに見える振る舞いは docs-first。** 外部仕様に書かれていないものは実装しない。
  必要なら先にドキュメントを書き、人間の承認を得てから実装する。
- **内部の作りはコードが正。** 実装の詳細を文書化しない代わりに、
  **実装前に関連コードを読む**（[ADR-0008](docs/adr/0008-code-as-source-of-truth.md)）。
- 記述言語は**日本語**。コード内の識別子・コミットの type は英語。

## 1. 書く前に読むもの

作業を始める前に、最低限これらを確認する。

1. [docs/spec/constraints.md](docs/spec/constraints.md) — 越えてはならない制約
2. 該当機能の [docs/spec/external/](docs/spec/external/) — ユーザーへの約束
3. [docs/spec/architecture.md](docs/spec/architecture.md) と
   [docs/guidelines/](docs/guidelines/) — 従うべき構造と規約
4. **関連するコード** — 内部の作りはここでしか分からない
5. 関連する [docs/adr/](docs/adr/) — 既に否決された案を蒸し返さないため

## 2. 情報をどこに書くか

| 書きたい内容 | 置き場所 |
| --- | --- |
| 「A ではなく B を採用する」という選択 | `docs/adr/NNNN-*.md`（新規作成） |
| ユーザーから見える振る舞い（画面・操作・表示・設定・通知） | `docs/spec/external/<機能グループ>.md` |
| 機能をまたいで効く構造（基本設計） | `docs/spec/architecture.md` |
| 機能に属さない横断ルール（規約・エラー処理・テスト方針） | `docs/guidelines/<名前>.md` |
| 要求そのものの追加・変更 | `docs/spec/requirements.md` |
| **個別機能の実現方法・処理フロー・データ構造** | **書かない。コードが正** |
| 調査結果・その場の方針など、**他人が読む必要があるもの** | **GitHub Issue**（`gh issue create`） |
| 書き捨ての作業メモ（**共有されない**） | `docs/notes/`（git 管理外。[ADR-0012](docs/adr/0012-local-notes-directory.md)） |
| 手順・進め方 | `CONTRIBUTING.md` |
| 反復する作業の型 | `.claude/skills/<name>/SKILL.md` |

判断基準は [docs/spec/README.md](docs/spec/README.md#文書とコードの境界) を正とする。

### コードが正である

[ADR-0008](docs/adr/0008-code-as-source-of-truth.md) により、**機能ごとの内部仕様は書かない。**

- 処理フロー・クラス構成・データ構造・API のパースは文書化しない。
  コードを読めば分かるものを書くと二重管理になり、必ず片方が古くなる。
- **したがって、実装前に関連コードを読むこと。**
  「仕様書に書いていないから存在しない」は**外部から見える振る舞いにのみ**適用され、
  内部構造には適用されない。
- 文書として維持するのは「古くなると誤解を生み、かつコードから読み取れないもの」だけ
  —— 基本設計（`docs/spec/architecture.md`）とガイドライン（`docs/guidelines/`）。

### 変更してよい範囲

仕様（要求層 / `architecture.md` / `external/`）とガイドライン（`docs/guidelines/`）の変更には
**人間の承認が必要**。承認なしに書き換えず、変更案を提示して確認を求めること。
ただし**相互リンクの追記と一覧表の同期は承認不要**（ここで作業を止めない）。

コード側は、外部仕様と基本設計を変えない限り自分の判断で実装してよい。

詳細な運用ルールは [docs/README.md](docs/README.md)。
`.claude/skills/` に `write-adr` / `write-spec` / `capture-issue` / `docs-audit` を用意しているので、
該当する作業ではスキルを呼び出すこと。

## 3. 禁止事項

- **事実の捏造をしない。** 艦これの API 仕様・レスポンス構造・パラメータは公式に公開されていない。
  推測で仕様書に書かず、必ず出典（実測ログ / 参照した OSS など）を併記する。
  出典が無い記述には `TODO(要検証)` を付ける。
- **同じ事実を 2 箇所に書かない。** 重複しそうならリンクで参照する。
- **ADR を書き換えない。** 決定を覆すときは新しい ADR を起票し、旧 ADR の
  ステータスを `Superseded by ADR-NNNN` に変更するだけに留める。
- **未確定の技術構成を既定路線として扱わない。** 技術選定は ADR で決まるまで未決である。
- 制約（[docs/spec/constraints.md](docs/spec/constraints.md)）に反する機能を提案・実装しない。
  特に**ゲームプレイの自動化・マクロ・多重リクエスト**は実装しない。

## 4. コミット規約

[Conventional Commits](https://www.conventionalcommits.org/ja/v1.0.0/) に従う。

```
<type>(<scope>): <日本語の要約>
```

| type | 用途 |
| --- | --- |
| `feat` | 機能追加 |
| `fix` | 不具合修正 |
| `docs` | ドキュメントのみの変更 |
| `refactor` | 挙動を変えない内部改善 |
| `test` | テストの追加・修正 |
| `chore` | ビルド・設定・依存関係 |

- ADR の追加は `docs(adr): ADR-0005 …` のように scope を付ける。
- 1 コミット 1 目的。仕様変更と実装を混ぜない。
- コミットメッセージ末尾に共著者行を付ける（Claude Code の既定に従う）。

### ブランチを切らない

**当分の間、`main` に直接コミットする**（[ADR-0014](docs/adr/0014-trunk-based-on-main.md)）。
feature ブランチも Pull Request も作らない。
エージェントの既定はブランチを切る挙動だが、**このリポジトリではそれを適用しない。**

`main` は常に公開されている。**壊れた状態で push しないこと。**

## 5. 作業の完了条件

実装タスクは以下がすべて満たされて初めて「完了」とする。

- [ ] 外部仕様（`docs/spec/external/`）と実装が一致している
- [ ] 基本設計に影響する変更なら `docs/spec/architecture.md` を更新した
- [ ] 新たに下した設計判断が ADR に記録されている
- [ ] 作業中に立てた Issue が閉じられている（放置しない）
- [ ] 未解決事項が仕様書の「未解決事項」節に明記されている

**内部仕様の更新は完了条件に含まれない。** コードが正であるため
（[ADR-0008](docs/adr/0008-code-as-source-of-truth.md)）。

## 6. 迷ったら

推測で進めず、**GitHub Issue に論点を書き出して**人間に確認を求める（`capture-issue` スキル）。
**曖昧なまま実装するより、質問するほうが安い。**

手元の作業メモは `docs/notes/` に置いてよい（git 管理外・共有されない）。
ただし**他人が読む必要があるものをそこに書かない。** 共有されないため、失われたのと同じになる。
それ以外の場所にリポジトリ内メモを作らない。
