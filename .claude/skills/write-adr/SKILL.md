---
name: write-adr
description: 技術的な意思決定を ADR として docs/adr/ に記録する。言語・フレームワーク・実行形態・ライブラリの選定、データの持ち方、外部依存の追加、通信方式、配布方法など、選択肢を実際に比較したうえで覆すコストが高い判断を下したときに使う。既存の決定を覆すときにも使う。指示をそのまま記録するだけのものや、まだ動いている検討には使わない。
---

# ADR を書く

## 使う場面

**次の 3 つをすべて満たすときだけ**。1 つでも欠けたら ADR にしない。

1. **選択肢を実際に比較した。** 却下案とその理由が事実として書ける
2. **覆すコストが高い。** 公開済み / 外部依存 / データ形式 / 法的なもの
3. **事情を知らない人が「なぜ？」と再提案しうる**

中心になるのは**技術的な判断**である —— 言語・フレームワーク・実行形態・
ライブラリの選定、データの持ち方と保存場所、外部依存の追加、通信の観測方式、配布方法。

## 使わない場面

| 内容 | 正しい置き場所 |
| --- | --- |
| ユーザーの指示・方針をそのまま記録するだけ | `CLAUDE.md` / `docs/` に**規範として**書く |
| まだ動いている検討の途中経過 | **GitHub Issue**。固まってから 1 本書く |
| 1 行の設定変更、簡単に戻せる判断 | 該当文書を直すだけ |
| 「いまどうなっているか」の説明 | `docs/spec/` / `docs/README.md` |

**迷ったら書かない。** 書かなかった決定は Issue と git 履歴に残るが、
質の低い ADR は将来のエージェントに誤った前提を与える。
詳細は [docs/adr/README.md](../../../docs/adr/README.md#adr-にする条件)。

## 手順

### 1. 既存 ADR を確認する

`docs/adr/README.md` の一覧を読む。

- 同じ論点の ADR が既にある → 新規作成ではなく、その ADR を Supersede する形にする
- 既に却下された案を再提案していないか確認する

### 2. 番号を決める

`docs/adr/` の最大番号 + 1。**欠番を作らない。**
ファイル名は `NNNN-english-kebab-title.md`（4 桁ゼロ埋め、英小文字ケバブ）。

### 3. テンプレートから書く

`docs/adr/template.md` をコピーする。書くときの注意:

- **「決定」節は断定形で 1〜3 文。** 読み手が最初に結論を得られること。
- **却下した案を必ず書く。** 却下理由が本体である。「なんとなく」は禁止。
- **却下案を捏造しない。** 実際に比較していない案を、テンプレートを埋めるために
  書いてはいけない。**却下理由が事実として書けないなら、それは ADR にする段階にない。**
  テンプレートの案 B / 案 C は埋めるべき欄ではなく、無ければ削る。
- **「決め手」でトレードオフを明示する。** 何を取って何を捨てたのか。
- 根拠が推測の場合は `TODO(要検証)` を付ける
  （艦これの API 仕様は非公開。[constraints.md](../../../docs/spec/constraints.md) C-03）。
- ステータスは **`Proposed` で作る。** 人間の承認を得るまで `Accepted` にしない。

### 4. 一覧を更新する

`docs/adr/README.md` の表に 1 行追加する。**忘れやすい。**

### 5. 関連文書を更新する

- 決定が仕様に影響する → `docs/spec/` の該当ファイルを更新する
- 元になった暫定メモがある → `docs/notes/` の該当メモを `昇格済み` にし、
  昇格先リンクを書いて `docs/notes/archive/` へ移動する

### 6. コミットする

```
docs(adr): ADR-NNNN <日本語の要約>
```

## 既存の決定を覆すとき

**旧 ADR の本文を書き換えてはいけない。**

1. 新しい ADR を起票する（「背景と課題」に、なぜ ADR-XXXX を覆すのか書く）
2. 旧 ADR の**ステータス行だけ**を `Superseded by ADR-NNNN` に変更する
3. 旧 ADR の他の記述には触れない

## チェックリスト

- [ ] 番号が連番で、欠番が無い
- [ ] 却下した案とその理由が書かれている
- [ ] 「決定」が断定形で書かれている
- [ ] ステータスが `Proposed`（人間の承認前）
- [ ] `docs/adr/README.md` の一覧を更新した
- [ ] 関連する spec / notes を更新した
