[package]
name = "harubridge-core"
version = "0.1.0"
description = "Harubridge のコア。通信データの解釈・状態保持・永続化を担う"
edition = "2021"
license = "MIT"

# このクレートは tauri に依存しない。
# ここに tauri を足してはいけない。IPC・ウィンドウの都合は src-tauri 側に置く。
[dependencies]

[build-dependencies]
serde_json = "1"
